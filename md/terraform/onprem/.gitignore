# Terraform state — 절대 커밋 X (시크릿 포함 가능)
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl

# 실제 환경 변수 (API 토큰 등)
terraform.tfvars
*.auto.tfvars

# CLI crash log
crash.log
crash.*.log

# Override
override.tf
override.tf.json
*_override.tf
*_override.tf.json
